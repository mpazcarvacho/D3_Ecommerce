
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Shop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Home" , root_path, class: "nav-link active" %>
            </li>
            <li class="nav-item">
              <%= link_to "Carrito" , cart_path, class: "nav-link" %>
            </li>
          </ul>
          
        </div>
        <div class="d-flex flex-row-reverse">
            <ul class="navbar-nav d-flex flex-row-reverse ">
              <%# DONE Check if current user is signed in to add link to sign in or sign out  %>
              <% if(!user_signed_in?)%>
                <li class="nav-item p-2">
                  <%= link_to "Sign In" , new_user_session_path, method: :delete, class: "nav-link " %>
                </li>
              <%end%>
              <%if (user_signed_in?)%>
                <li class="nav-item p-2">
                  <%= link_to "Sign Out" , destroy_user_session_path, method: :delete, class: "nav-link " %>
                </li>
                <li class="nav-item p-2" id="welcome">
                  <%= "Welcome #{current_user.email}"%>
                </li>
                <%end%>
            </ul>
          </div>
      </div>
    </nav>